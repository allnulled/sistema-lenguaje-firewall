[ rule 87 ]
on event { * } then {
    anytime [[ // load_everything() ]]
    if not [[ true ]] or [[ false ]] and (
        ([[ false ]] or [[ false ]]) or
        ([[ false ]] or [[ true ]])
    ) then [[ 
        // await go();
    ]]
    if [[ false ]] or [[ false ]] and (
        [[ false ]] or [[ false ]]
    ) then [[ 
        // await go();
    ]]
}

on event { * } then {
    anytime {{ // await cargar_todas_las_cosaj() }}
    if (not {{ true }} and not {{ true }} and {{ false }}) then {{
        // stay_high()
    }}
}

on event { * } then {
    anytime {{ }}
}

on event { * } then {{ // load_everything() }}

on event { * } then {{
    // console.clear();
    console.log("Evento: *" );
    console.log("[TRACE] A las " + (new Date()).toString() + "\n[TRACE] Evento emitido: " + evento);
    console.log("[TRACE]", parametros);
}}


[ Regla X ]
en evento { * } entonces {
    siempre [[ // cargar_todo() ]]
    si no [[ true ]] o [[ false ]] y (
        ([[ false ]] o [[ false ]]) o
        ([[ false ]] o [[ true ]])
    ) entonces [[ 
        // await go();
    ]]
    si [[ false ]] o [[ false ]] y (
        [[ false ]] o [[ false ]]
    ) entonces [[ 
        // await go();
    ]]
}

on event {super.hiper.mega.guay} then {
    anytime {{ console.log("Evento: super.hiper.mega.guay" ); }}
}
on event {super.hiper.mega.guay.*} then {
    anytime {{ console.log("Evento: super.hiper.mega.guay.*" ); }}
}
on event {super.hiper.mega.*} then {
    anytime {{ console.log("Evento: super.hiper.mega.*" ); }}
}
on event {super.hiper.*} then {
    anytime {{ console.log("Evento: super.hiper.*" ); }}
}
on event {super.*} then {
    anytime {{ console.log("Evento: super.*" ); }}
}